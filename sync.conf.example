# One target file per line. Each file must contain:
#   <!-- SHARED:BEGIN -->
#   <!-- SHARED:END -->
# sync.sh will replace the content between these markers with shared.md.
#
# Paths are absolute so the same sync.conf works regardless of cwd.
# Lines starting with # and blank lines are ignored.

/Users/luciuschen/.emacs.d/straight/repos/clutch/CLAUDE.md
/Users/luciuschen/repos/clutch-jdbc-agent/CLAUDE.md
